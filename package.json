{
  "name": "dspro0819-graykeep",
  "version": "0.2.0",
  "description": "DSH gray-test session preservation toolkit. graypin: one-command pin of your own gray-test identity into a DSH provider configuration with backup / rollback / round-trip verified YAML edits. Zero runtime dependencies.",
  "type": "module",
  "bin": {
    "graykeep": "bin/graykeep.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/pin.test.js"
  },
  "keywords": [
    "deepseek",
    "harness",
    "dsh",
    "gray-test",
    "canary",
    "config"
  ],
  "license": "MIT",
  "dependencies": {}
}
